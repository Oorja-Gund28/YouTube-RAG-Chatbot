# YouTube-RAG-Chatbot
Built an intelligent YouTube chatbot using Retrieval-Augmented Generation (RAG) and LangChain. The application extracts video transcripts, generates embeddings, stores them in a vector database, and uses an LLM to answer user questions with context-aware responses from the video content.
